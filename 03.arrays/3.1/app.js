(() => {
let tableau = [];
let number = [1, 2, 3, 4, 5];
for (let number of tableau) { 
    console.log(number);
}
    })();

    (() => {
        let tableau = [];
        let number = [101, 102, 103];
        for (let number of tableau) { 
            console.log(number);
        }
            })();